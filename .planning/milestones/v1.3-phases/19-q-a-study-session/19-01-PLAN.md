---
phase: 19-q-a-study-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/it.json
  - messages/es.json
  - src/__tests__/components/QAStudySession.test.tsx
autonomous: true
requirements:
  - QAFLOW-03
  - QAFLOW-04
  - QAFLOW-07
  - QAFLOW-08

must_haves:
  truths:
    - "i18n namespace 'study' in both it.json and es.json contains backToScenarios, scenarioDone, allScenariosDone keys"
    - "Test scaffold file exists with it.todo() stubs for all QAStudySession behaviors"
  artifacts:
    - path: "messages/it.json"
      provides: "Italian i18n keys for Q&A session UI"
      contains: "backToScenarios"
    - path: "messages/es.json"
      provides: "Spanish i18n keys for Q&A session UI"
      contains: "backToScenarios"
    - path: "src/__tests__/components/QAStudySession.test.tsx"
      provides: "Test scaffold with todo stubs for Plan 02 to implement"
      min_lines: 40
  key_links:
    - from: "messages/it.json"
      to: "src/app/[lang]/qa/[scenario]/QAStudySession.tsx"
      via: "useTranslations('study')"
      pattern: "backToScenarios|scenarioDone|allScenariosDone"
---

<objective>
Add three Q&A-specific i18n keys to both language message files and create the test scaffold that Plan 02 will fill in.

Purpose: QAStudySession.tsx needs `study.backToScenarios`, `study.scenarioDone`, and `study.allScenariosDone` at runtime. The test scaffold (using `it.todo()`) signals to Plan 02 exactly which behaviors must be covered before the component ships.
Output: Updated messages/it.json and messages/es.json; src/__tests__/components/QAStudySession.test.tsx with todo stubs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-q-a-study-session/19-CONTEXT.md
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From messages/it.json (existing study keys — add new keys alongside these):
```json
"study": {
  "tapToReveal": "Tocca per rivelare",
  "correct": "Corretto",
  "incorrect": "Sbagliato",
  "allDone": "Hai finito per oggi! Torna domani.",
  "backToDecks": "← Torna ai mazzi",
  "cardCounter": "Scheda {current} di {total}",
  "deckComplete": "Mazzo completato!",
  "studyAgain": "Studia ancora"
}
```

From messages/es.json (existing study keys — add new keys alongside these):
```json
"study": {
  "tapToReveal": "Toca para revelar",
  "correct": "Correcto",
  "incorrect": "Incorrecto",
  "allDone": "¡Terminado por hoy! Vuelve mañana.",
  "backToDecks": "← Volver a los mazos",
  "cardCounter": "Tarjeta {current} de {total}",
  "deckComplete": "¡Mazo completado!",
  "studyAgain": "Estudiar de nuevo"
}
```

New keys to add (decisions from 19-CONTEXT.md):
- `backToScenarios` — back button in QA session (Rephrase keeps `backToDecks`)
- `scenarioDone` — scenario-done heading
- `allScenariosDone` — all-done heading
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Q&A i18n keys to both message files</name>
  <files>messages/it.json, messages/es.json</files>
  <action>
Add three new keys to the `study` namespace in each file. Append them after `"studyAgain"` (last existing key) — do NOT reorder existing keys.

For messages/it.json add inside "study":
```json
"backToScenarios": "← Torna agli scenari",
"scenarioDone": "Scenario completato!",
"allScenariosDone": "Tutti gli scenari completati!"
```

For messages/es.json add inside "study":
```json
"backToScenarios": "← Volver a los escenarios",
"scenarioDone": "¡Escenario completado!",
"allScenariosDone": "¡Todos los escenarios completados!"
```

These keys are consumed by QAStudySession (Plan 02) via `useTranslations('study')`. Do NOT add a new namespace — `study` already exists.
  </action>
  <verify>
    <automated>node -e "const it=require('./messages/it.json'); const es=require('./messages/es.json'); ['backToScenarios','scenarioDone','allScenariosDone'].forEach(k => { if (!it.study[k]) throw new Error('it missing '+k); if (!es.study[k]) throw new Error('es missing '+k); }); console.log('OK');"</automated>
  </verify>
  <done>Both JSON files parse without error and contain all three new keys in the study namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Create QAStudySession test scaffold with it.todo() stubs</name>
  <files>src/__tests__/components/QAStudySession.test.tsx</files>
  <action>
Create the test file with `it.todo()` stubs for all behaviors Plan 02 must implement. Use the same mock patterns as `StudySession.test.tsx` (vi.mock for next-intl, next/link, useQASRS, useVoiceRecognition).

The file must include todo stubs for:
1. renders question text and audio button on card face
2. renders exactly 4 choice buttons (1 correct + 3 foils)
3. tapping correct choice shows correct feedback state
4. tapping incorrect choice shows incorrect feedback state
5. auto-advances to next card after 600ms delay
6. mic button appears when isSupported; spoken match triggers correct feedback
7. spoken non-match sets notRecognized feedback, no SRS update
8. shows scenarioDone screen after last card when another scenario has due cards
9. shows allScenariosDone screen after last card when all scenarios are empty
10. dueCards.length === 0 at mount shows done screen immediately
11. Study again button on scenarioDone screen resets session

Structure as a single `describe('QAStudySession', () => { ... })` block. All tests are `it.todo(description)` — no implementation yet. Plan 02 fills in the actual test bodies.

Use these module mock stubs at top of file (Plan 02 will configure return values per test):
```typescript
vi.mock('next-intl', () => ({
  useTranslations: () => (key: string, params?: Record<string, unknown>) => {
    if (params) return `${key}(${JSON.stringify(params)})`;
    return key;
  },
}));
vi.mock('@/hooks/useQASRS', () => ({ useQASRS: vi.fn() }));
vi.mock('@/hooks/useVoiceRecognition', () => ({ useVoiceRecognition: vi.fn() }));
vi.mock('next/link', () => ({
  default: ({ children, href }: { children: React.ReactNode; href: string }) => (
    <a href={href}>{children}</a>
  ),
}));
```
  </action>
  <verify>
    <automated>bun run test -- --reporter=verbose src/__tests__/components/QAStudySession.test.tsx 2>&1 | grep -E "todo|PASS|FAIL|ERROR"</automated>
  </verify>
  <done>Test file runs without parse errors. Vitest reports all 11 stubs as pending (todo) — zero failures, zero passes.</done>
</task>

</tasks>

<verification>
- Both message files have the three new keys in the study namespace
- JSON files remain valid (no syntax errors)
- Test scaffold exists with 11 it.todo() stubs
- `bun run test` passes (no regressions — scaffold adds 0 failures)
</verification>

<success_criteria>
- `messages/it.json` and `messages/es.json` each contain `study.backToScenarios`, `study.scenarioDone`, and `study.allScenariosDone`
- `src/__tests__/components/QAStudySession.test.tsx` exists with 11 todo stubs — 0 failures
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-q-a-study-session/19-01-SUMMARY.md`
</output>
