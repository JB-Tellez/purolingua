---
phase: 13-ui-i18n-and-styling
plan: "03"
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified: []
autonomous: false
requirements: [STYLE-01]

must_haves:
  truths:
    - "Visiting /it renders Italian UI labels — deck names, chip labels, and study controls are in Italian"
    - "Visiting /es renders Spanish UI labels — the same components show Spanish strings"
    - "LevelFilterChips are visible on the deck grid with A1 and A2 chips; clicking one toggles active state"
    - "Visiting /it/daily shows the AudioButton (speaker icon) on the card front; clicking it attempts TTS"
    - "Clicking ChoiceButton options after revealing a card applies correct (green) or incorrect (red) visual state"
    - "Visual appearance matches original purolingua.com Mediterranean design — no broken layouts or missing styles"
  artifacts:
    - path: "src/components/LevelFilterChips.tsx"
      provides: "Chip filter visible on deck grid"
    - path: "src/components/AudioButton.tsx"
      provides: "Speaker button on card front"
    - path: "src/components/ChoiceButton.tsx"
      provides: "Multiple-choice options on card back"
  key_links:
    - from: "Browser /it"
      to: "messages/it.json"
      via: "next-intl useTranslations resolving Italian strings"
    - from: "Browser /es"
      to: "messages/es.json"
      via: "next-intl useTranslations resolving Spanish strings"
---

<objective>
Human verification that all Phase 13 UI work is visually correct and locale-switching works end-to-end in the browser.

Purpose: Automated tests cannot verify visual appearance, TTS audio behavior, or interactive UI state. This checkpoint confirms Phase 13 ships with v1.1 visual parity before the milestone is closed.
Output: User approval that the build is visually correct and i18n switching works.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-ui-i18n-and-styling/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build and test verification</name>
  <files></files>
  <action>
    Run the full build and test suite to confirm all automation passes before human review.

    ```bash
    bun run build
    bun run test
    ```

    Confirm:
    - `bun run build` exits 0 with no TypeScript errors
    - `bun run test` passes all unit tests with no failures
    - `out/` directory contains 22+ static HTML files

    If either command fails, fix the issue before the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && bun run build 2>&1 | grep -E "error|Error|✓|successfully" | tail -5 && bun run test 2>&1 | grep -E "passed|failed|Tests" | tail -3</automated>
  </verify>
  <done>Build exits 0, all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 13 complete implementation:
    - LevelFilterChips: A1/A2 filter chips on deck grid (UI-01)
    - FlashCard: existing flip flow with AudioButton added to card front (UI-02, UI-04)
    - ChoiceButton: multiple-choice options appear after card reveal (UI-03)
    - next-intl: all UI labels come from messages/{lang}.json files (I18N-01, I18N-02)
    - Visual parity with original purolingua.com Mediterranean design (STYLE-01)
  </what-built>
  <how-to-verify>
    Run `bun run dev` then open http://localhost:3000 in a browser.

    **Test 1 — Italian deck grid:**
    1. Visit http://localhost:3000/it
    2. Confirm: deck names are in Italian (e.g. "Vita Quotidiana", "Ristorante")
    3. Confirm: "Scegli un mazzo" subtitle is visible (not "Choose a deck to practice")
    4. Confirm: A1 and A2 filter chips appear above the deck grid
    5. Click A1 chip — it should deactivate (greyed). Click again — it should reactivate (teal)
    6. With only A1 active, clicking a deck should only show A1-level cards

    **Test 2 — Spanish deck grid:**
    1. Visit http://localhost:3000/es
    2. Confirm: deck names are in Spanish ("Vida Diaria", "Restaurante", etc.)
    3. Confirm: "Elige un mazo" subtitle is visible
    4. Confirm: A1 and A2 filter chips appear above the deck grid

    **Test 3 — Study session (Italian):**
    1. Visit http://localhost:3000/it/daily
    2. Confirm: the Italian card front phrase is visible in serif font
    3. Confirm: the AudioButton (🔊) appears in the top-right corner of the card face
    4. Click the AudioButton — browser should attempt TTS (may be silent if no voices, but no JS errors)
    5. Click "Rivela la risposta" (not "Reveal Answer") to flip the card
    6. Confirm: 4 multiple-choice options appear below the card (ChoiceButton rows)
    7. Click the correct option — it should turn green; after 0.6s the next card loads
    8. Click an incorrect option on the next card — it should turn red
    9. Confirm: "← Torna ai mazzi" link is visible (not "Back to decks")

    **Test 4 — Study session (Spanish):**
    1. Visit http://localhost:3000/es/restaurant
    2. Confirm: controls say "Revelar respuesta", "Correcto", "Incorrecto"
    3. Confirm: AudioButton present on card front

    **Test 5 — Visual design:**
    1. Compare to original purolingua.com — Mediterranean palette (beige bg, colored deck cards, card flip)
    2. Confirm: no broken layouts, no unstyled raw text, no missing icons
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass. Or describe any issues found and Claude will fix them before re-running verification.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 13 is verified complete when:
- All automated tests pass (bun run build, bun run test)
- Human confirms locale switching works (IT vs ES strings differ)
- Human confirms LevelFilterChips toggle correctly with FLTR-06 guard
- Human confirms AudioButton is present and ChoiceButton shows correct/incorrect states
- Human confirms visual design matches v1.1 Mediterranean style
</verification>

<success_criteria>
- All 7 Phase 13 requirements met: UI-01, UI-02, UI-03, UI-04, I18N-01, I18N-02, STYLE-01
- Zero hardcoded English strings visible on /it or /es routes
- LevelFilterChips, AudioButton, ChoiceButton all render correctly in browser
- User types "approved" to close the milestone
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-i18n-and-styling/13-03-SUMMARY.md`
</output>
