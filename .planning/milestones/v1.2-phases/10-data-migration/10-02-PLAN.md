---
phase: 10-data-migration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/es/daily.ts
  - src/data/es/restaurant.ts
  - src/data/es/travel.ts
  - src/data/es/shopping.ts
  - src/data/es/hotel.ts
  - src/data/es/emergencies.ts
  - src/data/es/social.ts
  - src/data/es/weather.ts
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "All 8 Spanish deck files are importable TypeScript modules"
    - "Each deck exports a typed Card[] array with front, back, and level fields"
    - "No TypeScript errors — bun run build exits 0 with the new files present"
    - "Card counts match the source: 40 cards per deck (20 A2 + 20 A1 each)"
  artifacts:
    - path: "src/data/es/daily.ts"
      provides: "Spanish daily deck card array"
      exports: ["spanishDaily"]
    - path: "src/data/es/restaurant.ts"
      provides: "Spanish restaurant deck card array"
      exports: ["spanishRestaurant"]
    - path: "src/data/es/travel.ts"
      provides: "Spanish travel deck card array"
      exports: ["spanishTravel"]
    - path: "src/data/es/shopping.ts"
      provides: "Spanish shopping deck card array"
      exports: ["spanishShopping"]
    - path: "src/data/es/hotel.ts"
      provides: "Spanish hotel deck card array"
      exports: ["spanishHotel"]
    - path: "src/data/es/emergencies.ts"
      provides: "Spanish emergencies deck card array"
      exports: ["spanishEmergencies"]
    - path: "src/data/es/social.ts"
      provides: "Spanish social deck card array"
      exports: ["spanishSocial"]
    - path: "src/data/es/weather.ts"
      provides: "Spanish weather deck card array"
      exports: ["spanishWeather"]
  key_links:
    - from: "src/data/es/*.ts"
      to: "src/types/index.ts"
      via: "import type { Card } from '@/types'"
      pattern: "import.*Card.*@/types"
---

<objective>
Port all 8 Spanish vocabulary decks from the vanilla JS source into typed TypeScript modules under `src/data/es/`.

Purpose: Phase 10 requirement DATA-03 — Spanish card arrays must exist as typed TypeScript modules before Phase 11 (SRS hooks) and Phase 12 (routing) can consume them. Runs in parallel with Plan 01 (Italian) since no file overlap exists.

Output: 8 TypeScript files in `src/data/es/`, each exporting a `Card[]` constant typed against the `Card` interface from `@/types`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scaffold/09-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs. From src/types/index.ts (created in Phase 9). -->

From src/types/index.ts:
```typescript
export type Lang = 'it' | 'es';
export type Level = 'A1' | 'A2';
export type DeckId = 'daily' | 'restaurant' | 'travel' | 'shopping' | 'hotel' | 'emergencies' | 'social' | 'weather';

export interface Card {
  front: string;   // target language word/phrase
  back: string;    // translation/answer
  level: Level;    // CEFR level
}
```
</interfaces>

Source data lives on the `main` branch at `src/locales/es/decks.js`. Read it with:
```
git show main:src/locales/es/decks.js
```

The source file is a single JS array of 8 deck objects. Each deck has `id`, `title`, `description`, `icon`, `theme`, and `cards[]`. Only the `cards[]` array is needed for these data files — id, title, icon, theme go in the metadata module (Plan 03). Each card has `front`, `back`, and `level` matching the `Card` interface exactly.

Card counts (verified from source):
- Each deck: 40 cards total (20 A2 cards first, then 20 A1 cards appended)
- Spanish total: 320 cards across 8 decks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Spanish deck TypeScript modules</name>
  <files>
    src/data/es/daily.ts,
    src/data/es/restaurant.ts,
    src/data/es/travel.ts,
    src/data/es/shopping.ts,
    src/data/es/hotel.ts,
    src/data/es/emergencies.ts,
    src/data/es/social.ts,
    src/data/es/weather.ts
  </files>
  <action>
    Read the full source with `git show main:src/locales/es/decks.js`.

    For each of the 8 decks, create `src/data/es/{deckId}.ts` following this exact pattern:

    ```typescript
    import type { Card } from '@/types';

    export const spanishDaily: Card[] = [
      { front: '...', back: '...', level: 'A2' },
      // ... all 40 cards in original order (A2 first, A1 appended)
    ];
    ```

    Export name conventions (camelCase, "spanish" prefix + PascalCase deck name):
    - daily       → `spanishDaily`
    - restaurant  → `spanishRestaurant`
    - travel      → `spanishTravel`
    - shopping    → `spanishShopping`
    - hotel       → `spanishHotel`
    - emergencies → `spanishEmergencies`
    - social      → `spanishSocial`
    - weather     → `spanishWeather`

    Rules:
    - Copy ALL cards from the source in original order — do not reorder or skip
    - Preserve A2 cards first, A1 cards at end (positional indices are SRS keys)
    - Omit `id`, `title`, `description`, `icon`, `theme` — those go in Plan 03
    - Use single quotes for string values to match project TS style
    - No trailing commas on the array closing bracket (use trailing commas inside objects)
    - Each file must compile with `import type { Card } from '@/types'` — no other imports needed
    - Spanish strings may contain accented characters (á, é, í, ó, ú, ñ, ü, ¡, ¿) — copy them exactly
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 8 files exist in src/data/es/, each exports a named Card[] constant, bun run build exits 0 with no TypeScript errors related to the new files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify card counts</name>
  <files></files>
  <action>
    After creating the 8 files, run a quick count verification to confirm no cards were accidentally dropped. For each file, count the number of card objects:

    ```bash
    for f in src/data/es/*.ts; do echo "$f: $(grep -c "front:" $f) cards"; done
    ```

    Expected output: every deck shows exactly 40 cards.

    Also verify level distribution — each deck should have exactly 20 A2 and 20 A1:
    ```bash
    for f in src/data/es/*.ts; do
      echo "$f: A2=$(grep -c "'A2'" $f) A1=$(grep -c "'A1'" $f)"
    done
    ```

    If any file shows != 40 cards, re-read the source for that deck and fix. Do not proceed if counts are wrong.
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && for f in src/data/es/*.ts; do count=$(grep -c "front:" "$f"); if [ "$count" -ne 40 ]; then echo "FAIL: $f has $count cards (expected 40)"; exit 1; fi; done && echo "All 8 Spanish decks: 40 cards each"</automated>
  </verify>
  <done>
    All 8 Spanish deck files confirmed at exactly 40 cards each (20 A2 + 20 A1), matching the source.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` exits 0 — no TypeScript errors in new files
2. All 8 files exist: `ls src/data/es/` shows daily.ts, restaurant.ts, travel.ts, shopping.ts, hotel.ts, emergencies.ts, social.ts, weather.ts
3. Each file exports a named Card[] constant (grep for `export const spanish`)
4. All 8 decks have exactly 40 cards (Task 2 verification script)
5. Import type from '@/types' is used (not a value import)
</verification>

<success_criteria>
- 8 TypeScript files in src/data/es/ — one per deck
- Each file: `import type { Card } from '@/types'` + single named export of Card[]
- 320 total Spanish cards (8 × 40) preserved in original order
- `bun run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-migration/10-02-SUMMARY.md`
</output>
