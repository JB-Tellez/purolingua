---
phase: 17-qa-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
autonomous: true
requirements:
  - QADATA-01
  - QADATA-02
  - QAFLOW-06
must_haves:
  truths:
    - "QACard, ScenarioId, and Scenario types are importable from '@/types'"
    - "ScenarioId union literal covers all 7 scenario identifiers"
    - "QACard.foils is typed as a fixed-length tuple [string, string, string]"
    - "Scenario type includes level field for Phase 18 browser filtering"
  artifacts:
    - path: "src/types/index.ts"
      provides: "QACard, ScenarioId, Scenario type exports"
      contains: "export type QACard"
  key_links:
    - from: "src/types/index.ts"
      to: "src/data/qa/*.ts"
      via: "import type { QACard, Scenario }"
      pattern: "import.*QACard.*Scenario.*@/types"
    - from: "src/types/index.ts"
      to: "src/hooks/useQASRS.ts"
      via: "import type { Scenario, ScenarioId, Lang }"
      pattern: "import.*ScenarioId.*@/types"
---

<objective>
Add QACard, ScenarioId, and Scenario types to src/types/index.ts so that Plans 02 (content) and 03 (hook) can import against a concrete contract.

Purpose: Interface-first ordering — content files and the hook both import these types. Defining them first eliminates ambiguity and ensures Plans 02 and 03 can execute in parallel.
Output: Updated src/types/index.ts with three new exported types appended after existing types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-qa-data/17-CONTEXT.md

<interfaces>
<!-- Existing types in src/types/index.ts — append new types after these -->

From src/types/index.ts (current state):
```typescript
export type Lang = 'it' | 'es';
export type Level = 'A1' | 'A2';

export type DeckId =
  | 'daily' | 'restaurant' | 'travel' | 'shopping'
  | 'hotel' | 'emergencies' | 'social' | 'weather';

export interface Card {
  front: string;
  back: string;
  level: Level;
}

export interface Deck {
  id: DeckId;
  lang: Lang;
  cards: Card[];
}

export interface Progress {
  box: number;
  nextReview: string; // YYYY-MM-DD
}

export type ProgressRecord = Record<string, Progress>;
```

<!-- New types to add (from design doc + CONTEXT.md decisions) -->
New types needed:
```typescript
export type ScenarioId =
  | 'caffe' | 'albergo' | 'ristorante' | 'strada'
  | 'presentazioni' | 'negozio' | 'treno';

export interface QACard {
  id: string;                       // e.g. "caffe_01"
  question: string;                 // target language
  correct: string;                  // target language
  foils: [string, string, string];  // target language, fixed tuple
  level: Level;
}

export interface Scenario {
  id: ScenarioId;
  icon: string;      // emoji
  titleIt: string;   // e.g. "Al Caffè"
  titleEs: string;   // e.g. "En el Café"
  level: Level;      // scenario-level tag for Phase 18 filter
  cards: QACard[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QACard, ScenarioId, and Scenario types to src/types/index.ts</name>
  <files>src/types/index.ts</files>
  <action>
    Append three new exported types after the existing ProgressRecord export. Do NOT modify any existing type — only append.

    1. ScenarioId — union of all 7 scenario string literals in this exact order:
       'caffe' | 'albergo' | 'ristorante' | 'strada' | 'presentazioni' | 'negozio' | 'treno'

    2. QACard interface:
       - id: string (e.g. "caffe_01")
       - question: string (target language)
       - correct: string (target language)
       - foils: [string, string, string] (fixed-length tuple, target language)
       - level: Level (reuse existing Level type)

    3. Scenario interface:
       - id: ScenarioId
       - icon: string (emoji)
       - titleIt: string
       - titleEs: string
       - level: Level (scenario-level tag; all cards within a scenario share this level at launch)
       - cards: QACard[]

    Add a comment block before the new types: "// Q&A Mode types — added Phase 17"
    Keep the existing file header comment and all existing types intact.
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    `npx tsc --noEmit` exits 0. `src/types/index.ts` exports QACard, ScenarioId, and Scenario. All three are importable via `import type { QACard, ScenarioId, Scenario } from '@/types'`.
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero type errors. Confirm the three new type names appear in the exports of src/types/index.ts.
</verification>

<success_criteria>
- src/types/index.ts exports QACard, ScenarioId, Scenario alongside existing types
- ScenarioId union covers exactly the 7 scenario IDs
- QACard.foils is [string, string, string] tuple (not string[])
- Scenario.level field exists for Phase 18 browser filtering
- No existing types modified; tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-qa-data/17-01-SUMMARY.md`
</output>
