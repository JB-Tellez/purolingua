---
phase: 17-qa-data
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/data/qa/caffe.ts
  - src/data/qa/albergo.ts
  - src/data/qa/ristorante.ts
  - src/data/qa/strada.ts
  - src/data/qa/presentazioni.ts
  - src/data/qa/negozio.ts
  - src/data/qa/treno.ts
  - src/data/qa/index.ts
autonomous: true
requirements:
  - QADATA-01
  - QADATA-02
must_haves:
  truths:
    - "All 7 Italian scenario titles, icons, and card content are present and correctly typed"
    - "All 7 Spanish scenarios are parallel translations of the Italian content"
    - "Each scenario has exactly 8 cards with question, correct, and 3 foils"
    - "A1 scenarios (5) and A2 scenarios (2) are tagged with the correct level"
    - "src/data/qa/index.ts exports all 7 scenarios as an array"
  artifacts:
    - path: "src/data/qa/index.ts"
      provides: "barrel export of all 7 Scenario objects"
      exports: ["scenarios"]
    - path: "src/data/qa/caffe.ts"
      provides: "Al Caffè / En el Café scenario (A1)"
      contains: "export const caffe"
    - path: "src/data/qa/albergo.ts"
      provides: "In Albergo / En el Hotel scenario (A1)"
      contains: "export const albergo"
    - path: "src/data/qa/ristorante.ts"
      provides: "Al Ristorante / En el Restaurante scenario (A1)"
      contains: "export const ristorante"
    - path: "src/data/qa/strada.ts"
      provides: "Per Strada / En la Calle scenario (A1)"
      contains: "export const strada"
    - path: "src/data/qa/presentazioni.ts"
      provides: "Presentazioni / Presentaciones scenario (A1)"
      contains: "export const presentazioni"
    - path: "src/data/qa/negozio.ts"
      provides: "Al Negozio / En la Tienda scenario (A2)"
      contains: "export const negozio"
    - path: "src/data/qa/treno.ts"
      provides: "In Treno / En el Tren scenario (A2)"
      contains: "export const treno"
  key_links:
    - from: "src/data/qa/index.ts"
      to: "src/data/qa/*.ts"
      via: "named imports of each Scenario const"
      pattern: "import.*from.*./caffe"
---

<objective>
Author all 14 Q&A scenario data files: 7 Italian-primary scenario files (each containing both Italian and Spanish content fields) plus the index barrel. Each scenario exports a typed Scenario object with 8 hand-authored cards including question, correct response, and 3 contextually plausible foils.

Purpose: The full Q&A content layer — this is what users will actually study. Phase 18 and 19 UI renders these Scenario objects directly.
Output: src/data/qa/ directory with 7 scenario files + index.ts exporting all scenarios.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-qa-data/17-CONTEXT.md
@.planning/phases/17-qa-data/17-01-SUMMARY.md
@docs/plans/2026-03-08-qa-scenarios-design.md

<interfaces>
<!-- Types to import in every scenario file -->
From src/types/index.ts (added in Plan 01):
```typescript
import type { QACard, Scenario } from '@/types';

// ScenarioId values: 'caffe' | 'albergo' | 'ristorante' | 'strada' | 'presentazioni' | 'negozio' | 'treno'
// Level values: 'A1' | 'A2'

export interface QACard {
  id: string;                       // "{scenarioId}_{nn}" e.g. "caffe_01"
  question: string;                 // target language question
  correct: string;                  // target language correct response
  foils: [string, string, string];  // 3 target language wrong responses
  level: Level;                     // same as parent Scenario.level
}

export interface Scenario {
  id: ScenarioId;
  icon: string;      // emoji
  titleIt: string;
  titleEs: string;
  level: Level;      // 'A1' or 'A2' — all cards inherit this
  cards: QACard[];
}
```

<!-- Each scenario file pattern (mirror src/data/it/daily.ts style) -->
```typescript
// src/data/qa/caffe.ts
import type { Scenario } from '@/types';

export const caffe: Scenario = {
  id: 'caffe',
  icon: '☕',
  titleIt: 'Al Caffè',
  titleEs: 'En el Café',
  level: 'A1',
  cards: [
    {
      id: 'caffe_01',
      question: 'Buongiorno! Cosa desidera?',
      correct: 'Un cappuccino, per favore.',
      foils: ['Dov\'è il bagno?', 'Mi chiamo Luca.', 'A domani!'],
      level: 'A1',
    },
    // ... 7 more cards
  ],
};
```

<!-- Index barrel pattern (mirrors src/data/it/index.ts) -->
```typescript
// src/data/qa/index.ts
import { caffe } from './caffe';
import { albergo } from './albergo';
// ... all 7
export type { Scenario } from '@/types';
export const scenarios = [caffe, albergo, ristorante, strada, presentazioni, negozio, treno];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 A1 scenario files (caffe, albergo, ristorante, strada, presentazioni)</name>
  <files>src/data/qa/caffe.ts, src/data/qa/albergo.ts, src/data/qa/ristorante.ts, src/data/qa/strada.ts, src/data/qa/presentazioni.ts</files>
  <action>
    Create all 5 A1 scenario files. Each exports a single const of type Scenario with level: 'A1'.

    Card authoring rules:
    - 8 cards per scenario (ids: {scenarioId}_01 through {scenarioId}_08)
    - question: realistic conversational Italian the learner would hear in that setting
    - correct: the most natural Italian response to that question
    - foils[0], foils[1], foils[2]: plausible Italian responses from a DIFFERENT context (e.g., answering a coffee question with a hotel phrasing) — contextually wrong but grammatically natural
    - Spanish is NOT in these files — card content is Italian only; titleEs is the Spanish scenario title
    - level: 'A1' on every card

    Use the design doc sample content for tone calibration:
    - caffe: "Buongiorno! Cosa desidera?" → "Un cappuccino, per favore."
    - albergo: "Ha una prenotazione?" → "Sì, a nome Johnson."
    - ristorante: "Cosa prende come primo?" → "Per me gli spaghetti al pomodoro."
    - strada: "Scusi, dov'è la stazione?" → "È sempre dritto, poi gira a sinistra."
    - presentazioni: "Come ti chiami?" → "Mi chiamo Sara. E tu?"

    Extend each scenario to 8 cards following the same conversational register. All content must be target-language Italian — no English text in question/correct/foils fields.

    For each scenario (with the exception of strada which has a 1-card sample), build out cards that cover the natural arc of that conversational setting (arrival, ordering, paying, farewells, etc.).

    File structure (one per file):
    ```
    import type { Scenario } from '@/types';
    export const {scenarioId}: Scenario = { ... };
    ```
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    5 files exist in src/data/qa/. Each file exports a typed Scenario with exactly 8 QACards. tsc --noEmit exits 0. No English text appears in question/correct/foils fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the 2 A2 scenario files (negozio, treno) and create the index barrel</name>
  <files>src/data/qa/negozio.ts, src/data/qa/treno.ts, src/data/qa/index.ts</files>
  <action>
    Create the 2 A2 scenario files and the index barrel.

    A2 scenarios follow the same authoring rules as Task 1 but level: 'A2'. Slightly more complex vocabulary and multi-step exchanges appropriate for A2 learners:
    - negozio: "Posso aiutarla?" → "Sì, cerco una giacca." (shopping: sizing, payment, try-on, return policy)
    - treno: "È libero questo posto?" → "Sì, prego, si accomodi." (train: reservations, platform, delays, luggage)

    Use design doc samples as seed cards, extend to 8 per scenario with realistic shopping/train station exchanges.

    Then create src/data/qa/index.ts:
    ```typescript
    // src/data/qa/index.ts
    // Q&A scenario barrel — imports all 7 scenarios and exports as ordered array
    import { caffe } from './caffe';
    import { albergo } from './albergo';
    import { ristorante } from './ristorante';
    import { strada } from './strada';
    import { presentazioni } from './presentazioni';
    import { negozio } from './negozio';
    import { treno } from './treno';

    export { caffe, albergo, ristorante, strada, presentazioni, negozio, treno };
    export const scenarios = [caffe, albergo, ristorante, strada, presentazioni, negozio, treno];
    ```

    The array order is the canonical display order for Phase 18 scenario browser. A1 scenarios appear before A2.
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    src/data/qa/ contains 9 files (7 scenario files + index.ts). `import { scenarios } from '@/data/qa'` resolves. All 7 Scenario objects are in the scenarios array. tsc --noEmit exits 0.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0
2. `src/data/qa/` contains exactly 8 files (7 scenarios + index.ts)
3. Each scenario file: exactly 8 cards, all target-language Italian content, correct level tag
4. index.ts exports `scenarios` array with all 7 in A1-before-A2 order
</verification>

<success_criteria>
- 7 scenario files authored with 8 cards each (56 cards total for Italian)
- All cards typed correctly as QACard with no TypeScript errors
- foils are fixed-length [string, string, string] tuples
- 5 scenarios tagged A1, 2 tagged A2
- scenarios[] array in index.ts is importable and contains all 7 Scenario objects
</success_criteria>

<output>
After completion, create `.planning/phases/17-qa-data/17-02-SUMMARY.md`
</output>
