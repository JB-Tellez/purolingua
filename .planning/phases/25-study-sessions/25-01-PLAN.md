---
phase: 25-study-sessions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/nuxt/RephraseSession.test.ts
  - tests/nuxt/QASession.test.ts
autonomous: true
requirements:
  - UI-06
  - UI-07

must_haves:
  truths:
    - "RephraseSession.test.ts exists with failing stubs that define the contractual selectors"
    - "QASession.test.ts exists with failing stubs that define the contractual selectors"
    - "Both test files run and produce RED failures (not errors) against the placeholder pages"
  artifacts:
    - path: "tests/nuxt/RephraseSession.test.ts"
      provides: "Wave 0 failing stubs for UI-06 Rephrase session page"
      exports: []
    - path: "tests/nuxt/QASession.test.ts"
      provides: "Wave 0 failing stubs for UI-07 Q&A session page"
      exports: []
  key_links:
    - from: "tests/nuxt/RephraseSession.test.ts"
      to: "app/pages/rephrase/[deck].vue"
      via: "dynamic import"
      pattern: "import.*app/pages/rephrase/\\[deck\\]\\.vue"
    - from: "tests/nuxt/QASession.test.ts"
      to: "app/pages/qa/[scenario].vue"
      via: "dynamic import"
      pattern: "import.*app/pages/qa/\\[scenario\\]\\.vue"
---

<objective>
Write failing Wave 0 test stubs for both study session pages so that Plans 02 and 03 have a test contract to build against.

Purpose: Establish the selector contracts (CSS class names, data attributes) before implementation. Tests must be RED against the current placeholder pages and GREEN after Plans 02 and 03 complete.
Output: Two test files — RephraseSession.test.ts and QASession.test.ts — with failing assertions that match the expected DOM structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-study-sessions/25-RESEARCH.md
@.planning/phases/25-study-sessions/25-VALIDATION.md

@tests/nuxt/DeckGrid.test.ts
</context>

<interfaces>
<!-- Established test pattern from Phase 24 (DeckGrid.test.ts) -->

```typescript
// Pattern for per-test dynamic import (avoids module cache bleed between tests)
import { mountSuspended } from '@nuxt/test-utils/runtime'
import { describe, it, expect, beforeEach } from 'vitest'

describe('RephraseSession', () => {
  beforeEach(() => localStorage.clear())

  it('renders all-done screen when localStorage is empty', async () => {
    const { default: Page } = await import('../../app/pages/rephrase/[deck].vue')
    const wrapper = await mountSuspended(Page)
    // assertions on wrapper
  })
})
```

<!-- Expected selector contracts for RephraseSession -->
<!-- all-done screen:  [data-session-done]  with text matching study.allDone i18n key -->
<!-- deck-done screen: [data-deck-done] -->
<!-- active card:      [data-session-card] -->
<!-- ClientOnly check: wrapper.html() should contain client-side content (not just fallback) -->
<!-- card counter:     [data-card-counter] -->
<!-- progress bar:     [data-progress-bar] -->

<!-- Expected selector contracts for QASession -->
<!-- all-done screen:  [data-session-done] -->
<!-- active question:  [data-session-card] -->
<!-- choice buttons:   [data-choice] (4 per card) -->
<!-- ClientOnly check: wrapper.html() should contain client-side content -->
<!-- card counter:     [data-card-counter] -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test stubs for RephraseSession (UI-06)</name>
  <files>tests/nuxt/RephraseSession.test.ts</files>
  <behavior>
    - Test 1: all-done screen renders when localStorage is empty (0 due cards) — selector: [data-session-done]
    - Test 2: [data-session-card] and [data-card-counter] elements are present when cards are due
    - Test 3: [data-progress-bar] element is present when cards are due
    - Test 4: wrapper.html() does NOT contain the placeholder text "session placeholder" (confirms implementation replaced placeholder)
  </behavior>
  <action>
Create `tests/nuxt/RephraseSession.test.ts` following the established per-test dynamic import pattern from DeckGrid.test.ts. All four tests should fail (RED) against the current placeholder page at `app/pages/rephrase/[deck].vue`.

Key implementation notes:
- Use `beforeEach(() => localStorage.clear())` to ensure 0 due cards state
- Use per-test `await import('../../app/pages/rephrase/[deck].vue')` (dynamic import inside each `it()` block)
- Test 1: `wrapper.find('[data-session-done]').exists()` — should be true when localStorage is empty (no due cards)
- Test 2: Set up localStorage with a due card first, then check `wrapper.find('[data-session-card]').exists()` and `wrapper.find('[data-card-counter]').exists()`; skip this test with `it.skip` if mocking SRS state is complex — implementation will verify manually
- Test 3: Same setup as Test 2, check `wrapper.find('[data-progress-bar]').exists()`
- Test 4: `expect(wrapper.html()).not.toContain('session placeholder')` — simple regression guard
- For Tests 2 and 3: Because pre-populating SRS state in localStorage requires knowledge of the key format, use a simpler approach: pre-seed `localStorage.setItem('it-progress', JSON.stringify({}))` with an empty object (returns user, gets new-user defaults, which shows A1 cards due for new users by SRS rules). If empty object still yields 0 due cards, use `it.skip` with a comment explaining that full interaction tests come during implementation.

Run after creating: `bun run vitest --project nuxt run tests/nuxt/RephraseSession.test.ts`
All tests except test 4 should FAIL (RED). Test 4 also fails (placeholder text IS present).
  </action>
  <verify>
    <automated>bun run vitest --project nuxt run tests/nuxt/RephraseSession.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, runs without syntax errors, test suite reports at least 1 failing test (RED against placeholder)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test stubs for QASession (UI-07)</name>
  <files>tests/nuxt/QASession.test.ts</files>
  <behavior>
    - Test 1: all-done screen renders when localStorage is empty — selector: [data-session-done]
    - Test 2: [data-session-card] and 4 [data-choice] elements present when cards are due
    - Test 3: [data-card-counter] element present when cards are due
    - Test 4: wrapper.html() does NOT contain "session placeholder"
  </behavior>
  <action>
Create `tests/nuxt/QASession.test.ts` following the same per-test dynamic import pattern. All four tests should fail (RED) against the current placeholder page at `app/pages/qa/[scenario].vue`.

Key implementation notes:
- Use `beforeEach(() => localStorage.clear())` to ensure 0 due cards state
- Use per-test `await import('../../app/pages/qa/[scenario].vue')` (dynamic import inside each `it()` block)
- Test 1: `wrapper.find('[data-session-done]').exists()` should be true when localStorage is empty
- Test 2 and 3: Same approach as RephraseSession tests — use `it.skip` if pre-seeding QA SRS state is complex. Comment explaining full interaction tests are written during Plan 03 implementation.
- Test 4: `expect(wrapper.html()).not.toContain('session placeholder')` — regression guard

Run after creating: `bun run vitest --project nuxt run tests/nuxt/QASession.test.ts`
All tests should FAIL (RED).
  </action>
  <verify>
    <automated>bun run vitest --project nuxt run tests/nuxt/QASession.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, runs without syntax errors, test suite reports at least 1 failing test (RED against placeholder)</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
bun run vitest --project nuxt run tests/nuxt/RephraseSession.test.ts tests/nuxt/QASession.test.ts
```

Expected: Both suites run. At minimum, the `data-session-done` test and the "not placeholder" test fail on each page. No TypeScript compilation errors.
</verification>

<success_criteria>
- tests/nuxt/RephraseSession.test.ts exists and runs with at least 1 failing assertion
- tests/nuxt/QASession.test.ts exists and runs with at least 1 failing assertion
- No syntax or TypeScript errors in either file
- Selector contracts ([data-session-done], [data-session-card], [data-card-counter], [data-progress-bar], [data-choice]) are established in the test files as the authoritative contracts Plans 02 and 03 must satisfy
</success_criteria>

<output>
After completion, create `.planning/phases/25-study-sessions/25-01-SUMMARY.md`
</output>
