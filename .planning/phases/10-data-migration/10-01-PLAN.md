---
phase: 10-data-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/it/daily.ts
  - src/data/it/restaurant.ts
  - src/data/it/travel.ts
  - src/data/it/shopping.ts
  - src/data/it/hotel.ts
  - src/data/it/emergencies.ts
  - src/data/it/social.ts
  - src/data/it/weather.ts
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "All 8 Italian deck files are importable TypeScript modules"
    - "Each deck exports a typed Card[] array with front, back, and level fields"
    - "No TypeScript errors — bun run build exits 0 with the new files present"
    - "Card counts match the source: 40 cards per deck (20 A2 + 20 A1 each)"
  artifacts:
    - path: "src/data/it/daily.ts"
      provides: "Italian daily deck card array"
      exports: ["italianDaily"]
    - path: "src/data/it/restaurant.ts"
      provides: "Italian restaurant deck card array"
      exports: ["italianRestaurant"]
    - path: "src/data/it/travel.ts"
      provides: "Italian travel deck card array"
      exports: ["italianTravel"]
    - path: "src/data/it/shopping.ts"
      provides: "Italian shopping deck card array"
      exports: ["italianShopping"]
    - path: "src/data/it/hotel.ts"
      provides: "Italian hotel deck card array"
      exports: ["italianHotel"]
    - path: "src/data/it/emergencies.ts"
      provides: "Italian emergencies deck card array"
      exports: ["italianEmergencies"]
    - path: "src/data/it/social.ts"
      provides: "Italian social deck card array"
      exports: ["italianSocial"]
    - path: "src/data/it/weather.ts"
      provides: "Italian weather deck card array"
      exports: ["italianWeather"]
  key_links:
    - from: "src/data/it/*.ts"
      to: "src/types/index.ts"
      via: "import type { Card } from '@/types'"
      pattern: "import.*Card.*@/types"
---

<objective>
Port all 8 Italian vocabulary decks from the vanilla JS source into typed TypeScript modules under `src/data/it/`.

Purpose: Phase 10 requirement DATA-02 — Italian card arrays must exist as typed TypeScript modules before Phase 11 (SRS hooks) and Phase 12 (routing) can consume them.

Output: 8 TypeScript files in `src/data/it/`, each exporting a `Card[]` constant typed against the `Card` interface from `@/types`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scaffold/09-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs. From src/types/index.ts (created in Phase 9). -->

From src/types/index.ts:
```typescript
export type Lang = 'it' | 'es';
export type Level = 'A1' | 'A2';
export type DeckId = 'daily' | 'restaurant' | 'travel' | 'shopping' | 'hotel' | 'emergencies' | 'social' | 'weather';

export interface Card {
  front: string;   // target language word/phrase
  back: string;    // translation/answer
  level: Level;    // CEFR level
}
```
</interfaces>

Source data lives on the `main` branch at `src/locales/it/decks.js`. Read it with:
```
git show main:src/locales/it/decks.js
```

The source file is a single JS array of 8 deck objects. Each deck has `id`, `title`, `description`, `icon`, `theme`, and `cards[]`. Only the `cards[]` array is needed for these data files — id, title, icon, theme go in the metadata module (Plan 03). Each card has `front`, `back`, and `level` matching the `Card` interface exactly.

Card counts (verified from source):
- Each deck: 40 cards total (20 A2 cards first, then 20 A1 cards appended)
- Italian total: 320 cards across 8 decks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Italian deck TypeScript modules</name>
  <files>
    src/data/it/daily.ts,
    src/data/it/restaurant.ts,
    src/data/it/travel.ts,
    src/data/it/shopping.ts,
    src/data/it/hotel.ts,
    src/data/it/emergencies.ts,
    src/data/it/social.ts,
    src/data/it/weather.ts
  </files>
  <action>
    Read the full source with `git show main:src/locales/it/decks.js`.

    For each of the 8 decks, create `src/data/it/{deckId}.ts` following this exact pattern:

    ```typescript
    import type { Card } from '@/types';

    export const italianDaily: Card[] = [
      { front: '...', back: '...', level: 'A2' },
      // ... all 40 cards in original order (A2 first, A1 appended)
    ];
    ```

    Export name conventions (camelCase, "italian" prefix + PascalCase deck name):
    - daily       → `italianDaily`
    - restaurant  → `italianRestaurant`
    - travel      → `italianTravel`
    - shopping    → `italianShopping`
    - hotel       → `italianHotel`
    - emergencies → `italianEmergencies`
    - social      → `italianSocial`
    - weather     → `italianWeather`

    Rules:
    - Copy ALL cards from the source in original order — do not reorder or skip
    - Preserve A2 cards first, A1 cards at end (positional indices are SRS keys)
    - Omit `id`, `title`, `description`, `icon`, `theme` — those go in Plan 03
    - Use single quotes for string values to match project TS style
    - No trailing commas on the array closing bracket (use trailing commas inside objects)
    - Each file must compile with `import type { Card } from '@/types'` — no other imports needed
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 8 files exist in src/data/it/, each exports a named Card[] constant, bun run build exits 0 with no TypeScript errors related to the new files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify card counts</name>
  <files></files>
  <action>
    After creating the 8 files, run a quick count verification to confirm no cards were accidentally dropped. For each file, count the number of card objects:

    ```bash
    for f in src/data/it/*.ts; do echo "$f: $(grep -c "front:" $f) cards"; done
    ```

    Expected output: every deck shows exactly 40 cards.

    Also verify level distribution — each deck should have exactly 20 A2 and 20 A1:
    ```bash
    for f in src/data/it/*.ts; do
      echo "$f: A2=$(grep -c "'A2'" $f) A1=$(grep -c "'A1'" $f)"
    done
    ```

    If any file shows != 40 cards, re-read the source for that deck and fix. Do not proceed if counts are wrong.
  </action>
  <verify>
    <automated>cd /Users/jbtellez/genies/purolingua && for f in src/data/it/*.ts; do count=$(grep -c "front:" "$f"); if [ "$count" -ne 40 ]; then echo "FAIL: $f has $count cards (expected 40)"; exit 1; fi; done && echo "All 8 Italian decks: 40 cards each"</automated>
  </verify>
  <done>
    All 8 Italian deck files confirmed at exactly 40 cards each (20 A2 + 20 A1), matching the source.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` exits 0 — no TypeScript errors in new files
2. All 8 files exist: `ls src/data/it/` shows daily.ts, restaurant.ts, travel.ts, shopping.ts, hotel.ts, emergencies.ts, social.ts, weather.ts
3. Each file exports a named Card[] constant (grep for `export const italian`)
4. All 8 decks have exactly 40 cards (Task 2 verification script)
5. Import type from '@/types' is used (not a value import)
</verification>

<success_criteria>
- 8 TypeScript files in src/data/it/ — one per deck
- Each file: `import type { Card } from '@/types'` + single named export of Card[]
- 320 total Italian cards (8 × 40) preserved in original order
- `bun run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-migration/10-01-SUMMARY.md`
</output>
