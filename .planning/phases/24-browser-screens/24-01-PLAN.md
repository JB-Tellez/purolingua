---
phase: 24-browser-screens
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/nuxt/ActivityPicker.test.ts
  - tests/nuxt/DeckGrid.test.ts
  - tests/nuxt/ScenarioGrid.test.ts
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Test files exist and each failing test describes a specific observable behavior"
    - "Running `bun run vitest --project nuxt run` produces red failures — not import errors or syntax errors"
    - "Every behavior the implementation must satisfy is expressed as a test expectation"
  artifacts:
    - path: "tests/nuxt/ActivityPicker.test.ts"
      provides: "Contract tests for UI-03 — tile rendering and navigation"
    - path: "tests/nuxt/DeckGrid.test.ts"
      provides: "Contract tests for UI-04 — badge counts, chip toggle, FLTR-06 guard"
    - path: "tests/nuxt/ScenarioGrid.test.ts"
      provides: "Contract tests for UI-05 — scenario tile rendering, badge counts, FLTR-06 guard"
  key_links:
    - from: "tests/nuxt/ActivityPicker.test.ts"
      to: "app/pages/index.vue"
      via: "dynamic import inside each it() block"
      pattern: "import.*app/pages/index.vue"
    - from: "tests/nuxt/DeckGrid.test.ts"
      to: "app/pages/rephrase/index.vue"
      via: "dynamic import inside each it() block"
      pattern: "import.*app/pages/rephrase/index.vue"
    - from: "tests/nuxt/ScenarioGrid.test.ts"
      to: "app/pages/qa/index.vue"
      via: "dynamic import inside each it() block"
      pattern: "import.*app/pages/qa/index.vue"
---

<objective>
Write failing test stubs for all three browser screen pages before any implementation begins.

Purpose: Establish the Nyquist-compliant Wave 0 — tests define the observable contracts that Plans 02, 03, and 04 must satisfy. Running tests before implementation confirms they are RED (fail for the right reason, not due to import errors).

Output: Three test files in `tests/nuxt/` covering UI-03, UI-04, UI-05, and FLTR-06 guard in both grid pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-browser-screens/24-RESEARCH.md
</context>

<interfaces>
<!-- Established test pattern from Phase 23 -->
<!-- Follow the per-test dynamic import pattern from tests/nuxt/LevelFilterChips.test.ts -->

```typescript
// Pattern: dynamic import inside each it() block
import { mountSuspended } from '@nuxt/test-utils/runtime'
import { describe, it, expect } from 'vitest'

describe('ComponentName', () => {
  it('describes observable behavior', async () => {
    const { default: Page } = await import('../../app/pages/index.vue')
    const wrapper = await mountSuspended(Page)
    // assertions
  })
})
```

<!-- Pages under test (all currently placeholders) -->
// app/pages/index.vue        → ActivityPicker (UI-03)
// app/pages/rephrase/index.vue → DeckGrid (UI-04)
// app/pages/qa/index.vue     → ScenarioGrid (UI-05)

<!-- Key data selectors to test against -->
// ActivityPicker: two .deck-card elements, NuxtLinkLocale hrefs /rephrase and /qa
// DeckGrid: .deck-grid > .deck-card elements, .deck-card-badge on each card,
//           LevelFilterChips present (data-level-filter), chip toggle emits update
// ScenarioGrid: same grid+badge structure as DeckGrid, LevelFilterChips present
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test stubs for ActivityPicker, DeckGrid, and ScenarioGrid</name>
  <files>
    tests/nuxt/ActivityPicker.test.ts,
    tests/nuxt/DeckGrid.test.ts,
    tests/nuxt/ScenarioGrid.test.ts
  </files>
  <behavior>
    ActivityPicker.test.ts:
    - Test 1: renders two activity tiles (two .deck-card elements present)
    - Test 2: Rephrase tile has an anchor with href containing "/rephrase"
    - Test 3: Q&A tile has an anchor with href containing "/qa"

    DeckGrid.test.ts:
    - Test 1: renders at least one .deck-card element
    - Test 2: each .deck-card contains a .deck-card-badge element
    - Test 3: a [data-level-filter] element is present (LevelFilterChips)
    - Test 4: badge count is a number (not NaN, not empty string)

    ScenarioGrid.test.ts:
    - Test 1: renders at least one .deck-card element
    - Test 2: each .deck-card contains a .deck-card-badge element
    - Test 3: a [data-level-filter] element is present (LevelFilterChips)
    - Test 4: badge count is a number (not NaN, not empty string)
  </behavior>
  <action>
    Create three test files following the per-test dynamic import pattern from `tests/nuxt/LevelFilterChips.test.ts` exactly.

    ACTIVITYPICKER.TEST.TS:
    - Import: `mountSuspended` from `@nuxt/test-utils/runtime`, `describe/it/expect` from `vitest`
    - Dynamic import `../../app/pages/index.vue` inside each `it()` block
    - Mount with `mountSuspended(Page)` (no props — page reads locale from composable)
    - Test selectors: `wrapper.findAll('.deck-card')`, `wrapper.find('a[href*="/rephrase"]')`, `wrapper.find('a[href*="/qa"]')`

    DECKGRID.TEST.TS:
    - Dynamic import `../../app/pages/rephrase/index.vue` inside each `it()` block
    - Mount with `mountSuspended(Page)` (no props)
    - Test selectors:
      - `wrapper.findAll('.deck-card')` — length >= 1
      - Each card: `card.find('.deck-card-badge')` — exists
      - `wrapper.find('[data-level-filter]')` — exists (add `data-level-filter` attr to LevelFilterChips wrapper or the chips container in the page template)
      - Badge text: `badge.text()` — `.match(/^\d+$/)` or text === '✓'

    SCENARIOGRID.TEST.TS:
    - Dynamic import `../../app/pages/qa/index.vue` inside each `it()` block
    - Mount with `mountSuspended(Page)` (no props)
    - Same selectors as DeckGrid.test.ts (same observable structure)

    NOTE on data-level-filter: The LevelFilterChips component from Phase 23 wraps in `<div class="level-filter-chips">`. For test discoverability, Plan 03 and Plan 04 implementations will wrap `<LevelFilterChips>` in a `<div data-level-filter>` in the page template. Write the test expecting `[data-level-filter]` to be present.

    IMPORTANT: These tests MUST be RED when run against the current placeholder pages. Verify by running:
    `bun run vitest --project nuxt run tests/nuxt/ActivityPicker.test.ts`
    If tests pass against the placeholder, the test is not specific enough — tighten the selector.
  </action>
  <verify>
    <automated>bun run vitest --project nuxt run tests/nuxt/ActivityPicker.test.ts tests/nuxt/DeckGrid.test.ts tests/nuxt/ScenarioGrid.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    All three test files exist. Running the vitest command produces failures (not syntax/import errors). Each failure message names a missing selector or missing element, confirming tests are RED for the right reason.
  </done>
</task>

</tasks>

<verification>
`bun run vitest --project nuxt run tests/nuxt/ActivityPicker.test.ts tests/nuxt/DeckGrid.test.ts tests/nuxt/ScenarioGrid.test.ts` — all three files run, all tests FAIL (RED), no syntax or import errors.
</verification>

<success_criteria>
- Three test files created in `tests/nuxt/`
- Tests are RED (fail) against current placeholder pages
- No TypeScript/import errors in the test output
- Each test file has at least 3 failing `it()` cases
</success_criteria>

<output>
After completion, create `.planning/phases/24-browser-screens/24-01-SUMMARY.md`
</output>
