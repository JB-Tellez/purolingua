---
phase: 24-browser-screens
plan: 02
type: execute
wave: 1
depends_on:
  - 24-01
files_modified:
  - app/pages/index.vue
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "The home page shows two activity tiles: Rephrase and Q&A"
    - "Each tile is a locale-prefixed link — clicking from /it navigates to /it/rephrase or /it/qa"
    - "Tile text uses i18n keys from the 'activities' namespace (no hardcoded strings)"
  artifacts:
    - path: "app/pages/index.vue"
      provides: "ActivityPicker page — two NuxtLinkLocale tiles with icon, title, description"
      exports: []
  key_links:
    - from: "app/pages/index.vue"
      to: "/rephrase and /qa routes"
      via: "NuxtLinkLocale to='/rephrase' and to='/qa'"
      pattern: "NuxtLinkLocale.*to.*rephrase|NuxtLinkLocale.*to.*qa"
    - from: "app/pages/index.vue"
      to: "messages/it.json activities namespace"
      via: "useI18n() t('activities.rephrase.title') etc."
      pattern: "t\\(.activities\\."
---

<objective>
Implement the ActivityPicker screen by replacing the placeholder `app/pages/index.vue` with a full Vue SFC that renders two locale-aware navigation tiles.

Purpose: Satisfies UI-03 — the entry point of the app where users choose between Rephrase and Q&A activities.

Output: `app/pages/index.vue` — Vue SFC with `<script setup>`, `useI18n`, two `NuxtLinkLocale` tiles, Tailwind styling matching v1.3 design.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-browser-screens/24-RESEARCH.md
@.planning/phases/24-browser-screens/24-01-SUMMARY.md
</context>

<interfaces>
<!-- Source component being ported -->
<!-- src/components/ActivityPicker.tsx renders two tiles using ACTIVITIES const -->
// ACTIVITIES = [
//   { key: 'rephrase', icon: '🃏', path: 'rephrase' },
//   { key: 'qa', icon: '💬', path: 'qa' },
// ]

<!-- i18n keys available in messages/it.json and messages/es.json -->
// activities.rephrase.title      → "Rephrase"
// activities.rephrase.description → "Pratica frasi con le flashcard"
// activities.qa.title            → "Dialogo"
// activities.qa.description      → "Rispondi a domande in situazioni reali"

<!-- NuxtLinkLocale pattern confirmed from SiteHeader.vue (Phase 23) -->
// <NuxtLinkLocale to="/rephrase"> auto-prefixes current locale
// No :locale prop needed for activity tiles (uses current locale automatically)

<!-- CSS classes from v1.3 source -->
// .activity-grid  — outer grid container
// .deck-card      — individual tile card
// .deck-icon-circle — icon wrapper circle
// Tailwind classes used in the source for layout
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ActivityPicker in app/pages/index.vue</name>
  <files>app/pages/index.vue</files>
  <behavior>
    - Test 1 GREEN: wrapper.findAll('.deck-card') has length 2
    - Test 2 GREEN: wrapper.find('a[href*="/rephrase"]') exists
    - Test 3 GREEN: wrapper.find('a[href*="/qa"]') exists
  </behavior>
  <action>
    Replace the placeholder content of `app/pages/index.vue` with a full `<script setup>` + `<template>` SFC.

    SCRIPT SETUP:
    ```vue
    <script setup lang="ts">
    const { t } = useI18n()

    const ACTIVITIES = [
      { key: 'rephrase', icon: '🃏', path: '/rephrase' },
      { key: 'qa',       icon: '💬', path: '/qa' },
    ] as const
    </script>
    ```

    TEMPLATE:
    - Outer `<main>` with section header (`<h1>` using `t('page.title')`, `<p class="subtitle">` using `t('page.subtitle')`)
    - `<div class="activity-grid">` wrapping the tiles
    - `v-for="activity in ACTIVITIES"` over the array
    - Each tile: `<NuxtLinkLocale :to="activity.path">` wrapping a `<div class="deck-card">`
    - Inside each card: `<div class="deck-icon-circle">{{ activity.icon }}</div>`, `<h3>{{ t('activities.' + activity.key + '.title') }}</h3>`, `<p>{{ t('activities.' + activity.key + '.description') }}</p>`
    - Use `:key="activity.key"` on the NuxtLinkLocale element

    STYLE: No `<style>` block needed — Tailwind utility classes inline or rely on existing global CSS classes from v1.3 (`activity-grid`, `deck-card`, `deck-icon-circle`). Check `src/css/app.css` for existing class definitions — do NOT duplicate them.

    IMPORTANT: Use `NuxtLinkLocale` (not `NuxtLink`) so locale auto-prefixes. Do NOT hardcode `/it/rephrase` or `/es/rephrase`.
  </action>
  <verify>
    <automated>bun run vitest --project nuxt run tests/nuxt/ActivityPicker.test.ts</automated>
  </verify>
  <done>
    All ActivityPicker tests GREEN. `bun run vitest --project nuxt run tests/nuxt/ActivityPicker.test.ts` exits 0 with 3 passing tests.
  </done>
</task>

</tasks>

<verification>
`bun run vitest --project nuxt run tests/nuxt/ActivityPicker.test.ts` — all tests GREEN.
`bun run vitest --project nuxt run` — full suite still green (no regressions).
</verification>

<success_criteria>
- `app/pages/index.vue` renders two activity tiles using `NuxtLinkLocale`
- All ActivityPicker unit tests pass
- Full vitest suite passes with no regressions
- No hardcoded locale prefixes in href attributes
</success_criteria>

<output>
After completion, create `.planning/phases/24-browser-screens/24-02-SUMMARY.md`
</output>
