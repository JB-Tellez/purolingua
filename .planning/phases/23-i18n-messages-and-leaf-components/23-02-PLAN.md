---
phase: 23-i18n-messages-and-leaf-components
plan: 02
type: execute
wave: 1
depends_on:
  - 23-01
files_modified:
  - app/components/ChoiceButton.vue
  - app/components/AudioButton.vue
  - app/components/MicButton.vue
  - app/components/FeedbackMessage.vue
  - app/components/LevelFilterChips.vue
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "ChoiceButton renders text, emits click on body click and speak on speaker span click/keydown"
    - "AudioButton speak() has import.meta.client guard; button has aria-label from t('study.audio')"
    - "MicButton emits press; state-based CSS class applied correctly"
    - "FeedbackMessage renders nothing when state=null; correct color class per state"
    - "LevelFilterChips renders A1/A2 chips with active class; emits update:activeLevels on toggle"
    - "bun run test --project nuxt passes all 5 leaf component test files"
  artifacts:
    - path: "app/components/ChoiceButton.vue"
      provides: "Choice quiz button with embedded speaker icon"
      exports: []
    - path: "app/components/AudioButton.vue"
      provides: "TTS trigger button with SSR-safe speak()"
      exports: []
    - path: "app/components/MicButton.vue"
      provides: "Voice recognition trigger button"
      exports: []
    - path: "app/components/FeedbackMessage.vue"
      provides: "Display-only colored feedback paragraph"
      exports: []
    - path: "app/components/LevelFilterChips.vue"
      provides: "A1/A2 level filter chip group with v-model:activeLevels"
      exports: []
  key_links:
    - from: "LevelFilterChips.vue"
      to: "useLevelFilter (Phase 24)"
      via: "v-model:activeLevels binding"
      pattern: "emit.*update:activeLevels"
    - from: "AudioButton.vue"
      to: "window.speechSynthesis"
      via: "speak() with import.meta.client guard"
      pattern: "import.meta.client"
---

<objective>
Implement the five leaf Vue SFCs — ChoiceButton, AudioButton, MicButton, FeedbackMessage, LevelFilterChips — turning the RED tests from Plan 01 GREEN.

Purpose: These are the atomic UI primitives consumed by study session pages (Phase 25). They must exist before screen assembly (Phase 24).
Output: Five .vue files in app/components/ with all Plan 01 tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-i18n-messages-and-leaf-components/23-CONTEXT.md
@.planning/phases/23-i18n-messages-and-leaf-components/23-RESEARCH.md
@.planning/phases/23-i18n-messages-and-leaf-components/23-01-SUMMARY.md

<interfaces>
<!-- Types and patterns required by these components -->

From app/types/index.ts:
```typescript
export type Lang = 'it' | 'es'
export type Level = 'A1' | 'A2'
```

i18n keys used in these components:
- t('study.audio')     → "Ascolta" (it) / "Escucha" (es)
- t('study.correct')   → "Corretto" (it) / "Correcto" (es)
- t('study.incorrect') → "Sbagliato" (it) / "Incorrecto" (es)
- t('study.heard')     → "Sentito" (it) / "Escuchado" (es)
- t('study.notRecognized') → "Non riconosciuto" (it) / "No reconocido" (es)
- t('filter.label')    → "Livello" (it) / "Nivel" (es)
- t('filter.chips.A1') → "A1" (both)
- t('filter.chips.A2') → "A2" (both)

SSR guard pattern (locked Phase 20):
```typescript
if (!import.meta.client) return
```

Composable auto-import: useI18n() — no explicit import needed
Component auto-import: all files in app/components/ are globally available
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ChoiceButton, MicButton, FeedbackMessage</name>
  <files>app/components/ChoiceButton.vue, app/components/MicButton.vue, app/components/FeedbackMessage.vue</files>
  <behavior>
    ChoiceButton:
    - `defineProps<{ text: string; state: 'idle' | 'correct' | 'incorrect'; disabled?: boolean }>()`
    - `defineEmits<{ click: []; speak: [] }>()`
    - Root element: `<button type="button" :disabled="disabled || state !== 'idle'" :class="['quiz-btn', { [state]: state !== 'idle' }]" @click="emit('click')">`
    - Speaker span inside button: `<span role="button" class="quiz-audio-icon" :tabindex="0" @click.stop="emit('speak')" @keydown.enter.stop="emit('speak')" @keydown.space.stop="emit('speak')">🔊</span>`
    - Button text after span: `{{ text }}`
    - Does NOT use `useI18n()` — no i18n keys in this component

    MicButton:
    - `defineProps<{ state: 'idle' | 'listening' | 'error' }>()`
    - `defineEmits<{ press: [] }>()`
    - Root element: `<button type="button" :class="\`mic-btn\${state !== 'idle' ? \` mic-btn--\${state}\` : ''}\`" :aria-label="state === 'listening' ? 'Listening\u2026' : 'Start speaking'" :aria-pressed="state === 'listening'" @click.stop="emit('press')">🎙</button>`

    FeedbackMessage:
    - `defineProps<{ state: FeedbackState }>()` where `type FeedbackState = 'correct' | 'incorrect' | 'heard' | 'notRecognized' | null`
    - `const { t } = useI18n()`
    - `const COLOR_CLASS: Record<Exclude<FeedbackState, null>, string> = { correct: 'text-green-600', incorrect: 'text-red-500', heard: 'text-blue-500', notRecognized: 'text-gray-500' }`
    - Template: `<p v-if="state !== null" role="status" aria-live="polite" :class="\`text-sm font-medium text-center \${COLOR_CLASS[state!]}\`">{{ t(\`study.\${state}\`) }}</p>`
    - Multi-root template is fine in Vue 3; wrap in `<template>` if needed
  </behavior>
  <action>
    Create the three .vue files. Each uses `<script setup lang="ts">`.

    ChoiceButton: No useI18n() needed. The text prop is passed from the parent. The speaker span uses @click.stop and @keydown.enter.stop and @keydown.space.stop to prevent event bubbling to the button body.

    MicButton: No useI18n() needed. The aria-label is hardcoded English (matches the React source — no i18n key exists for this).

    FeedbackMessage: Uses useI18n(). The `study.${state}` dotted key maps to the correct message file key. The state! assertion is safe because v-if="state !== null" guards the template.

    After creating the three files, run the tests to verify GREEN:
    `bun run test --project nuxt tests/nuxt/ChoiceButton.test.ts tests/nuxt/MicButton.test.ts tests/nuxt/FeedbackMessage.test.ts`

    Fix any failures before proceeding to Task 2.
  </action>
  <verify>
    <automated>bun run test --project nuxt tests/nuxt/ChoiceButton.test.ts tests/nuxt/MicButton.test.ts tests/nuxt/FeedbackMessage.test.ts</automated>
  </verify>
  <done>Three test files pass GREEN. ChoiceButton emits click/speak correctly. MicButton emits press and applies state classes. FeedbackMessage hides on null and shows correct color class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AudioButton and LevelFilterChips</name>
  <files>app/components/AudioButton.vue, app/components/LevelFilterChips.vue</files>
  <behavior>
    AudioButton:
    - `defineProps<{ phrase: string; lang: 'it' | 'es' }>()`
    - No emits — speak() fires directly
    - `const { t } = useI18n()`
    - `const LANG_LOCALE: Record<string, string> = { it: 'it-IT', es: 'es-ES' }` (inline, not shared — single consumer)
    - `speak()` function with `if (!import.meta.client) return` guard at the top
    - speak() logic: `window.speechSynthesis.cancel()`, new SpeechSynthesisUtterance(phrase), set lang, rate=0.9, find best voice by lang prefix
    - Button renders with: `type="button"`, `:aria-label="t('study.audio')"`, `@click="speak()"`

    LevelFilterChips:
    - `import type { Level } from '~/types'`
    - `defineProps<{ activeLevels: Level[] }>()`
    - `defineEmits<{ 'update:activeLevels': [levels: Level[]] }>()`
    - `const { t } = useI18n()`
    - `const LEVELS: Level[] = ['A1', 'A2']`
    - `toggle(level: Level)` function: if included → emit with level removed; if not → emit with level added
    - NO FLTR-06 guard here — guard lives in useLevelFilter.setActiveLevels() (Phase 22)
    - Template: `<div class="level-filter" :aria-label="t('filter.label')">` containing `v-for="level in LEVELS"` buttons with `:class="\`filter-chip\${activeLevels.includes(level) ? ' active' : ''}\`"` and `@click="toggle(level)"`
  </behavior>
  <action>
    Create app/components/AudioButton.vue and app/components/LevelFilterChips.vue.

    AudioButton: The speak() function follows the same LANG_LOCALE pattern as useVoiceRecognition.ts. The import.meta.client guard is the FIRST line of speak() — before any window access. Button click calls speak() directly (no emit needed).

    LevelFilterChips: The emit event name is `'update:activeLevels'` (with quotes in defineEmits) which enables `v-model:activeLevels` on parent. The component emits a new array — never mutates props.

    After creating files run the tests:
    `bun run test --project nuxt tests/nuxt/AudioButton.test.ts tests/nuxt/LevelFilterChips.test.ts`

    Then run the full leaf component suite:
    `bun run test --project nuxt tests/nuxt/ChoiceButton.test.ts tests/nuxt/AudioButton.test.ts tests/nuxt/MicButton.test.ts tests/nuxt/FeedbackMessage.test.ts tests/nuxt/LevelFilterChips.test.ts`

    Fix any failures before completing.
  </action>
  <verify>
    <automated>bun run test --project nuxt tests/nuxt/ChoiceButton.test.ts tests/nuxt/AudioButton.test.ts tests/nuxt/MicButton.test.ts tests/nuxt/FeedbackMessage.test.ts tests/nuxt/LevelFilterChips.test.ts</automated>
  </verify>
  <done>All five leaf component test files pass GREEN. AudioButton has import.meta.client guard. LevelFilterChips emits update:activeLevels with correct array. bun run test passes without watch flags.</done>
</task>

</tasks>

<verification>
`bun run test --project nuxt tests/nuxt/ChoiceButton.test.ts tests/nuxt/AudioButton.test.ts tests/nuxt/MicButton.test.ts tests/nuxt/FeedbackMessage.test.ts tests/nuxt/LevelFilterChips.test.ts` — all pass.

Five .vue files exist in app/components/.

AudioButton.vue contains `import.meta.client` (not `typeof window`).

LevelFilterChips.vue contains `emit('update:activeLevels'` and does NOT contain FLTR-06 guard logic.
</verification>

<success_criteria>
All five leaf component tests GREEN. Five .vue files committed to app/components/. No FLTR-06 guard duplication in LevelFilterChips. import.meta.client guard in AudioButton.
</success_criteria>

<output>
After completion, create `.planning/phases/23-i18n-messages-and-leaf-components/23-02-SUMMARY.md`
</output>
