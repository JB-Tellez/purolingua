---
phase: 23-i18n-messages-and-leaf-components
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/nuxt/ChoiceButton.test.ts
  - tests/nuxt/AudioButton.test.ts
  - tests/nuxt/MicButton.test.ts
  - tests/nuxt/FeedbackMessage.test.ts
  - tests/nuxt/LevelFilterChips.test.ts
  - tests/nuxt/SiteHeader.test.ts
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Six test files exist in tests/nuxt/ and each fails with import/component-not-found (not syntax error)"
    - "Each test file describes the component contract: emits, props, and render behaviors"
    - "bun run test --project nuxt exits non-zero with clear test-not-passing output"
  artifacts:
    - path: "tests/nuxt/ChoiceButton.test.ts"
      provides: "ChoiceButton contract tests (click, speak emits; disabled state)"
    - path: "tests/nuxt/AudioButton.test.ts"
      provides: "AudioButton contract tests (aria-label from i18n, SSR guard)"
    - path: "tests/nuxt/MicButton.test.ts"
      provides: "MicButton contract tests (press emit, state-based class)"
    - path: "tests/nuxt/FeedbackMessage.test.ts"
      provides: "FeedbackMessage contract tests (null hides, colored text per state)"
    - path: "tests/nuxt/LevelFilterChips.test.ts"
      provides: "LevelFilterChips contract tests (update:activeLevels emit, chip toggle)"
    - path: "tests/nuxt/SiteHeader.test.ts"
      provides: "SiteHeader contract tests (logo text, back button visibility, locale switcher)"
  key_links:
    - from: "tests/nuxt/*.test.ts"
      to: "app/components/*.vue"
      via: "dynamic import in mountSuspended"
      pattern: "import.*app/components/"
---

<objective>
Write six failing test files — one per component — that define the contracts Plan 02 and Plan 03 must satisfy. Tests are RED until the components exist.

Purpose: Nyquist rule compliance. Every component task in Plans 02–03 must have a pre-existing test file. Writing them first locks the interface contracts before implementation.
Output: Six test files in tests/nuxt/ that fail with import or component errors (not syntax errors).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-i18n-messages-and-leaf-components/23-CONTEXT.md
@.planning/phases/23-i18n-messages-and-leaf-components/23-RESEARCH.md
@.planning/phases/23-i18n-messages-and-leaf-components/23-VALIDATION.md

<interfaces>
<!-- Key types the tests will use. -->

From app/types/index.ts:
```typescript
export type Lang = 'it' | 'es'
export type Level = 'A1' | 'A2'
```

Existing test pattern (from tests/nuxt/smoke.test.ts):
```typescript
import { mountSuspended } from '@nuxt/test-utils/runtime'
import { describe, it, expect } from 'vitest'

const { default: ComponentName } = await import('../../app/components/ComponentName.vue')
const wrapper = await mountSuspended(ComponentName, { props: { ... } })
```

Test run command: `bun run test --project nuxt tests/nuxt/FILENAME.test.ts`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test stubs for leaf components (ChoiceButton, AudioButton, MicButton, FeedbackMessage, LevelFilterChips)</name>
  <files>tests/nuxt/ChoiceButton.test.ts, tests/nuxt/AudioButton.test.ts, tests/nuxt/MicButton.test.ts, tests/nuxt/FeedbackMessage.test.ts, tests/nuxt/LevelFilterChips.test.ts</files>
  <behavior>
    ChoiceButton:
    - renders the `text` prop inside the button
    - emits `click` when the button body is clicked
    - emits `speak` when the speaker span is clicked
    - button is disabled when `disabled` prop is true
    - button is disabled when `state` is not 'idle'
    - speaker span has @keydown.enter and @keydown.space that emit `speak`

    AudioButton:
    - renders a button with aria-label containing t('study.audio')
    - does NOT crash during SSR (speak() returns early if !import.meta.client)

    MicButton:
    - renders mic icon button
    - emits `press` when clicked
    - adds `mic-btn--listening` class when state='listening'
    - adds `mic-btn--error` class when state='error'
    - no modifier class when state='idle'

    FeedbackMessage:
    - renders nothing (no element) when state is null
    - renders a `<p role="status">` when state is 'correct'
    - renders a `<p role="status">` when state is 'incorrect'
    - has `text-green-600` class when state='correct'
    - has `text-red-500` class when state='incorrect'
    - has `text-blue-500` class when state='heard'
    - has `text-gray-500` class when state='notRecognized'

    LevelFilterChips:
    - renders two buttons (A1 and A2)
    - A1 button has `active` class when 'A1' is in activeLevels
    - A2 button has `active` class when 'A2' is in activeLevels
    - clicking A1 when active emits `update:activeLevels` with A1 removed
    - clicking A2 when not active emits `update:activeLevels` with A2 added
  </behavior>
  <action>
    Create each test file using the established mountSuspended pattern from tests/nuxt/smoke.test.ts.

    File structure for each:
    ```
    import { mountSuspended } from '@nuxt/test-utils/runtime'
    import { describe, it, expect } from 'vitest'

    describe('ComponentName', () => {
      it('...', async () => {
        const { default: ComponentName } = await import('../../app/components/ComponentName.vue')
        const wrapper = await mountSuspended(ComponentName, { props: { ... } })
        expect(...).toBe(...)
      })
    })
    ```

    These files reference components that DO NOT EXIST YET — tests will fail with a module-not-found or import error. That is correct and expected (RED phase).

    ChoiceButton props: `{ text: string, state: 'idle' | 'correct' | 'incorrect', disabled?: boolean }`
    AudioButton props: `{ phrase: string, lang: 'it' | 'es' }`
    MicButton props: `{ state: 'idle' | 'listening' | 'error' }`
    FeedbackMessage props: `{ state: 'correct' | 'incorrect' | 'heard' | 'notRecognized' | null }`
    LevelFilterChips props: `{ activeLevels: Level[] }` where `Level = 'A1' | 'A2'`

    Do NOT create the .vue files — only the test files.

    For emit assertions use `wrapper.emitted('eventName')` from @vue/test-utils, which is available via mountSuspended.

    For LevelFilterChips import: `import type { Level } from '../../app/types'`
  </action>
  <verify>
    <automated>bun run test --project nuxt tests/nuxt/ChoiceButton.test.ts tests/nuxt/AudioButton.test.ts tests/nuxt/MicButton.test.ts tests/nuxt/FeedbackMessage.test.ts tests/nuxt/LevelFilterChips.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Five test files exist; each fails with import or component error (not TypeScript/syntax error); test names describe component behavior</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test stub for SiteHeader</name>
  <files>tests/nuxt/SiteHeader.test.ts</files>
  <behavior>
    SiteHeader:
    - renders logo text from i18n (t('nav.logo'))
    - does NOT show back button when route path is '/' (1 segment or less)
    - does NOT show back button when route path is '/it' (1 meaningful segment — just locale)
    - shows back button when route has depth >= 2 (e.g., '/it/rephrase')
    - back button links to parent path ('/it' when on '/it/rephrase')
    - renders a locale switcher button/trigger
    - reset button is present in the DOM
  </behavior>
  <action>
    Create tests/nuxt/SiteHeader.test.ts using the same mountSuspended pattern.

    SiteHeader props: `{ lang: 'it' | 'es' }`

    For path-depth tests, use the `route` option in mountSuspended:
    ```typescript
    const wrapper = await mountSuspended(SiteHeader, {
      props: { lang: 'it' },
      route: '/it/rephrase'
    })
    ```

    If the `route` option is not supported in this version of @nuxt/test-utils, fall back to testing only with the default route (no back button on '/').

    The component does not exist yet — test will fail with import error (RED phase is correct).

    Do NOT create the .vue file.
  </action>
  <verify>
    <automated>bun run test --project nuxt tests/nuxt/SiteHeader.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>tests/nuxt/SiteHeader.test.ts exists; fails with import or component error (not syntax error); test names describe locale switcher, back button, and logo rendering</done>
</task>

</tasks>

<verification>
All six test files exist in tests/nuxt/:
- ChoiceButton.test.ts, AudioButton.test.ts, MicButton.test.ts, FeedbackMessage.test.ts, LevelFilterChips.test.ts, SiteHeader.test.ts

Each fails with a module-not-found or component error — NOT a TypeScript parse error.

`bun run test --project nuxt` exits non-zero.
</verification>

<success_criteria>
Six RED test files committed. `bun run test --project nuxt` fails with import/component errors (not syntax errors). Plans 02 and 03 have test contracts to satisfy.
</success_criteria>

<output>
After completion, create `.planning/phases/23-i18n-messages-and-leaf-components/23-01-SUMMARY.md`
</output>
