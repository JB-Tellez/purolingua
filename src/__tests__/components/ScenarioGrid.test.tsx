import { describe, it } from 'vitest';

// Wave 0 — test scaffold for QAFLOW-01
// ScenarioGrid component does not exist yet (created in Plan 03).
// These todos will turn green when the component is implemented.

describe('ScenarioGrid', () => {
  it.todo('renders 7 scenario tiles');
  it.todo('shows due count badge when dueCards.length > 0');
  it.todo('shows checkmark badge when dueCards.length === 0');
});
