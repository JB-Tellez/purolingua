import { describe, it } from 'vitest';

// Wave 0 — test scaffold for ACTPICK-01
// ActivityPicker component does not exist yet (created in Plan 02).
// These todos will turn green when the component is implemented.

describe('ActivityPicker', () => {
  it.todo('renders Rephrase card with href /it/rephrase');
  it.todo('renders Q&A card with href /it/qa');
});
